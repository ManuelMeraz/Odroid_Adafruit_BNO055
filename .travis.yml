language: c++
sudo: false

git:
  depth: false
  quiet: true

before_install:
  - rm -rf ./examples_processing # we're not testing processing sketches
  - cd ~ && git clone https://github.com/hardkernel/wiringPi.git && sudo && cd wiringPi && ./build
  - cd ~ && git clone https://github.com/ManuelMeraz/OdroidGPIO.gita && cd OdroidGPIO && mkdir build && cd build && cmake .. && sudo make -j6 install
  - cd ~ && git clone https://github.com/ManuelMeraz/Odroid_Adafruit_Sensor.git && cd Odroid_Adafruit_Sensor && mkdir build && cmake .. && sudo make -j6 install

install:
  - cd ~/Odroid_Adafruit_BNO055 && mkdir build && cd build && cmake .. && sudo make -j6 install

